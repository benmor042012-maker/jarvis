{
  "name": "jarvis",
  "version": "3.0.0",
  "private": true,
  "license": "MIT",
  "description": "JARVIS — a self-contained local AI assistant and Windows computer-control agent. No cloud, no accounts, no API keys, no payments.",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "ai": "node scripts/install-ai.mjs",
    "start": "node scripts/start.mjs",
    "headless": "node scripts/start.mjs --headless",
    "build": "npm --prefix client run build",
    "build:installer": "npm --prefix client run build && npm --prefix desktop run build:win",
    "test": "npm --prefix desktop test",
    "lint": "npm --prefix client run lint",
    "typecheck": "npm --prefix client run typecheck",
    "verify": "node scripts/verify.mjs"
  }
}
